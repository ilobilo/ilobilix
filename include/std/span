// Copyright (C) 2022  ilobilo

#pragma once

#include <initializer_list>
#include_next <span>

namespace std
{
    template<typename Type>
    auto make_span(initializer_list<Type> ilist)
    {
        return span<Type>(const_cast<Type*>(ilist.begin()), ilist.size());
    }
} // namespace std